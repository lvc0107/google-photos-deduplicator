# Google Photos Deduplicator

## Features
- Hash + perceptual + AI duplicate detection
- Safe deletion (dry-run mode)
- Daily automation with GitHub Actions

## Run locally
pip install -r requirements.txt
python app/main.py
